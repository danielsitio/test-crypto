export default class Criptocurrencie {
    name: string
    symbol: string
    image: string
    constructor(name: string, symbol: string, image: string) {
        this.name = name
        this.symbol = symbol
        this.image = image
    }
} 